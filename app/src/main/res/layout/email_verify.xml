<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/title_verify_email"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/verify_email_title"
        android:textSize="@dimen/text_size_title"
        android:textAlignment="center"

        android:layout_marginTop="@dimen/title_top_margin"
        android:layout_marginLeft="@dimen/margin"
        android:layout_marginRight="@dimen/margin"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/verify_email_head"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="@string/Forgot_password_email"

        android:layout_margin="@dimen/margin"

        app:layout_constraintTop_toBottomOf="@id/title_verify_email"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"

        app:startIconDrawable="@drawable/mail_icon"
        android:textColorHint="@color/primary"
        app:startIconTint="@color/primary"
        >

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/verify_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textEmailAddress"
            android:background="@color/white"
            />

    </com.google.android.material.textfield.TextInputLayout>

    <Button
        android:id="@+id/verify_email_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send Link"
        android:textSize="18sp"
        android:background="@drawable/button_bg"
        android:layout_margin="@dimen/margin"
        app:layout_constraintTop_toBottomOf="@id/verify_email_head"
        app:layout_constraintLeft_toLeftOf="@id/verify_email_head"
        app:layout_constraintRight_toRightOf="@id/verify_email_head"
        />

</androidx.constraintlayout.widget.ConstraintLayout>